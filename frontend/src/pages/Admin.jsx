import React, { useEffect, useState } from 'react'
import { UserPlus, Users } from 'lucide-react'
import { api, json } from '../api'

export default function Admin(){const [students,setStudents]=useState([]),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const load=()=>json('/students').then(setStudents).catch(e=>setMessage(e.message));useEffect(load,[]);async function submit(e){e.preventDefault();setBusy(true);setMessage('');try{await api('/students',{method:'POST',body:new FormData(e.currentTarget)});e.currentTarget.reset();setMessage('Student registered and face embedding saved.');load()}catch(err){setMessage(err.message)}finally{setBusy(false)}}return <><header><div><p className="eyebrow">ADMINISTRATION</p><h1>Student registry</h1><p>Register consented photographs and manage local identities.</p></div></header><div className="admin-grid"><section className="panel form-panel"><h2><UserPlus/> Register student</h2><form onSubmit={submit} className="student-form"><label>Student ID<input name="student_id" required/></label><label>Full name<input name="name" required/></label><label>Department<input name="department" required/></label><label>Academic year<input name="year" type="number" min="1" max="8" required/></label><label className="wide">Registered photograph<input name="photo" type="file" accept="image/jpeg,image/png" required/><small>Use one clear, front-facing face with consent.</small></label>{message&&<div className="notice wide">{message}</div>}<button className="primary wide" disabled={busy}>{busy?'Processing face…':'Register student'}</button></form></section><section className="panel"><h2><Users/> Registered <span>{students.length}</span></h2><div className="student-list">{students.map(s=><article key={s.id}><img src={`http://localhost:8000${s.photo_url}`}/><div><b>{s.name}</b><small>{s.student_id} · {s.department} · Year {s.year}</small></div></article>)}{!students.length&&<p className="no-data">No students registered yet.</p>}</div></section></div></>}
